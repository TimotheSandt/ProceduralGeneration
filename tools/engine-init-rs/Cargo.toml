[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
zip       = { version = "2", default-features = false, features = ["deflate"] }
ureq      = { version = "2", features = ["json"] }
serde_json = "1"

[build-dependencies]
zip = { version = "2", default-features = false, features = ["deflate"] }
